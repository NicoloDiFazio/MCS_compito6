import ROOT as R
import numpy as np

min = 0.3
max = 0.5
n = 20
T = 25.16

x  = np.array([])
y  = np.array([])
ex = np.array([])
ey = np.array([])

file = open('out.dat','r')

for line in file:
    str = line.split()
    if len(str)>0:
        V = np.append(V,float(str[0]))
        I = np.append(I,float(str[1]))
        eI = np.append(eI,float(str[2]))

gr = TGraphErrors(len(V),V,I,0,eI)
gr.Draw("AP")
gApplication.Run(True)

"""
f = R.TF1("f", "[0]*(exp([1]*x/[2])-1)", min, max)

f.SetParameter(0, 1e-09)
f.SetParameter(1, 1.16e4)
f.FixParameter(2, T)

g.Draw()
f.Draw("SAME")

R.gApplication.Run(True)
"""
